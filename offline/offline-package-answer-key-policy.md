# Offline Package Answer-Key Policy

Modes are Omit, Include at End, Separate File and Release Online Later. Bundled keys require stable question IDs, validation and policy review and are labelled Practice Answer Key. Delayed keys require a separate later release and cannot be bundled. CSS, scripts, hidden pages, embedded JSON or passwords never count as withholding. Active examinations and restricted banks are not exportable.
