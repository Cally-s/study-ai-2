# Learning Mode Accessibility

> Step 36 adds accessible setup, one-question controls, source comparison, claim/workflow text alternatives, feedback/review/summary announcements, 200%/320px reflow, bilingual/RTL, TTS/STT, and answer-key isolation from the accessibility tree.

> Step 35 adds accessible answer/reasoning/evidence/scope inputs, challenge-type and hypothetical announcements, defend-or-revise actions, history/final-position views, 200%/320px reflow, bilingual/RTL, TTS/STT, and Low-Bandwidth support.

> Step 34 adds four labelled inputs, import/STT confirmation, step selection, six ordered review headings, repair/verification controls and version history.

> Step 33 exposes ordered section headings, provenance, comparison, transfer/waiting state and explicit support actions in accessible text.

> One-question, waiting, correction, final-explanation, hint, prerequisite and escalation states are announced in reading order and support reviewed STT input.

> The progressive card exposes exact full labels, purpose, waiting question/actions, final-level announcement, and text-only state.

> Runtime waiting/restriction/fallback announcements contain only permitted content; blocked answers never enter DOM, accessibility trees, print, or exports.

The labelled native select supports keyboard and screen readers, retains the full current label, uses 44-pixel controls, reflows on mobile and at 200% zoom, and respects high contrast and reduced motion. Shared Language Bridge behavior supplies bilingual labels and RTL layout. Low-Bandwidth mode uses the same text control without decorative dependencies.
