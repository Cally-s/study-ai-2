# AI Coach Composer

> Submission now feeds a server snapshot into Step 30 runtime resolution; ordinary text cannot change or end the governing contract.

The composer contains the exact “Mode: Coach Me” default selector directly beside the input. Mode changes preserve draft text, require an intentional control action, and are snapshotted by the server when a message is accepted.
