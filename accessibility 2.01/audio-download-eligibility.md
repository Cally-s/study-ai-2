# Audio Download Eligibility

Downloadable audio is unavailable in the current browser-only static build. The UI must not show a fake download action.

A future server provider may allow only eligible lesson summaries that are authorized, not an active assessment, not held for review, and contain no private or hidden data. Generated audio must be private by default, versioned to its source, expire under retention policy, and be regenerated when the source version changes.
