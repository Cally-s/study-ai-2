# Assumption Corrections

This assumption is wrong preserves the original and asks what to use instead. Add missing context requests only task-relevant fields and permits unknown values. Trusted policy, source, tool, document, and course conflicts are validated; student statements do not become externally confirmed facts. Corrections create versions, pass privacy checks, show impacts, and do not rewrite the response.
