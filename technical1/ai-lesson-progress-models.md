# AI Lesson Progress Models

Lesson definitions are versioned separately from `AILessonProgress`. A progress row identifies user, lesson version, course/assignment context, current position, and synchronization state. Attempts have unique sequence numbers and immutable submitted states. Attempt counts, completion, status, and assessment results are server derived. Accessibility and offline contracts remain referenced by version rather than copied into progress rows.
