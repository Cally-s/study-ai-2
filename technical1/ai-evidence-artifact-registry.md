# AI Evidence Artifact Registry

The registry gives every eligible artifact a stable ID, exact version, owner/team, source system and record, provenance, privacy classification, language, safe summary, and lifecycle. `STUDENT_CREATED` cannot be used for AI-generated work. Evidence records cite the exact artifact version and never replace the underlying artifact. Submission freezes the artifact version used for assessment; superseding creates a new version rather than rewriting history.
