# Data Security

Block confidential data before content transfer; quarantine/local-preflight metadata/schema where feasible; treat files and fields as untrusted; enforce purpose/version gate checks on uploads, connectors, embeddings, indexes, model calls, and exports; validate access and complete deletion; protect secrets; use idempotency, tenant isolation, and privacy-safe logs.
