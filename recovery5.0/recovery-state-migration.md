# Recovery State Migration

Inventory legacy case/task/action/plan/wait/error states before mapping. Map unambiguous values to layered enums; create wait/attention/system records rather than overload lifecycle. Preserve ambiguous records for human review, state history, partial progress and allocations. Add constraints after validation; dual-read only as needed; rollback restores old reads. No migration executed because no database exists.
