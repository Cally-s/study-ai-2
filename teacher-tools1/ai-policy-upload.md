# AI Policy Upload

Uploads are untrusted input. The service scans files, validates type and size, extracts text safely, flags prompt-injection-like content, and stores a private draft. A human compares extracted text with the original. Uploading never makes a policy binding: publication and precedence remain in the Step 53 policy builder.

