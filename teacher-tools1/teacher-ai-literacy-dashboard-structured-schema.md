# Dashboard Structured Schema

`teacher-ai-literacy-dashboard/1.0` is a separate strict schema containing the snapshot scope, completeness/suppression status, nine section summary arrays, instructional needs, actions, warnings, expiry, and source watermark. Unknown fields are rejected. Section summaries preserve validated evidence provenance. Private content, usage proxies, student ranks, hidden behavioral fields, and prohibited detector/risk fields are rejected.
