# Plain-Language Explanations

Step 23 reuses the canonical plain-language system and adds an InstructionMeaningContract requirement so batch drafts preserve every required action, condition, warning and academic element.

“Explain more simply” is an optional native-button command for approved educational explanations. It supports Standard Academic, Plain Language, Beginner Friendly, Step-by-Step Academic, Detailed Technical and Define Academic Terms modes. The original is never overwritten; tabs expose original, adapted and reflowing comparison views.

Adaptations shorten structure and reduce only allowlisted non-essential jargon while preserving the source version, course/grade, academic level, objective contract, required terms/definitions/reasoning, numbers, dates, names, units, equations, conditions, exceptions, warnings and citations. Examples are never invented. Active answers and authoritative high-stakes content fail closed. Temporary use does not save a preference; “Save this as my default” is explicit.

The static workspace has no AI or semantic-equivalence provider. Results are limited deterministic transformations plus exact contract validation and are never labelled academically equivalent or human reviewed. English only is supported. The original remains the fallback offline and after validation failure.
# Step 11 canonical mapping

Beginner Friendly, Plain Language, Standard Academic and Detailed Technical now map to Levels 1–4. Step-by-step and glossary-first are structures. All requests still enter this same Step 10 original-source contract and validation service; no duplicate explanation engine was added.
# Step 12 integration

Language Bridge requests reuse the selected Step 11 level, which delegates to this Step 10 original-source contract pipeline. Simplifying a bilingual fixture changes the shared level and never translates/simplifies from a prior translated version.
# Step 13 vocabulary cards

Cards may present separate approved plain and academic definitions but do not rewrite the Step 10 explanation or change its contract. Unknown definitions are not generated. The canonical English term remains visible at every presentation level.
# Step 14 simpler English

Use Simpler English explicitly calls the original-source Step 11/10 pipeline one level lower; it never simplifies from translation. English and selected-language levels are labelled separately and the split is not an ability/proficiency inference.
# Step 15 offline generation

Original text remains immediate; new generation requires the local page/service and is not claimed offline. Valid in-memory adaptations may remain during the open session. Low-bandwidth never lowers academic level or removes terms.
Step 17 package adaptations reuse AcademicContentContract invariants. Unsupported semantic validation fails closed; formatting cannot reduce curriculum content.
Step 19 Short duration never authorizes lower curriculum expectations. A script must pass the same AcademicContentContract or fail safely.
Step 21 Make Simpler delegates to validated plain-language adaptation from the original response/version; it never adapts an already transformed variant.
