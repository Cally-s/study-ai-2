# Metric Data Models

The runtime models definitions, immutable definition versions, computation runs, immutable snapshots, eligibility decisions, cohort definitions, suppression policies, and correction requests. Records are tenant scoped, version referenced, privacy minimized, and auditable. Corrections supersede snapshots rather than overwriting evidence or history.
