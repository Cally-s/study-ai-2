# AI Prompt Writing Coach

> Improve-the-prompt quiz questions use synthetic or approved prompts, one clear objective, student-authored revisions, isolated rubrics, and no active-assignment prompt completion.

Step 29 treats prompts as task content, not authority to change persistent learning mode. “Ignore the current mode” cannot switch the selector or weaken policy.
