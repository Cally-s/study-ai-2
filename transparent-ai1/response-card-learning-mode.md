# Response Card Learning Mode

The visible mode comes from an immutable trusted snapshot, never model text. The card may show the Assistance Ladder level, active checkpoint, and a plain-language policy limitation. Mode variants preserve Steps 29–41; changes require intentional student action and sub-workflows do not silently replace the main mode.
