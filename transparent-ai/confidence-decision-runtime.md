# Confidence Decision Runtime

Trusted services resolve the final label from evidence directness, verification, currentness, source independence/agreement, assumption impact, ambiguity, missing context, and policy. Models may propose concise wording but cannot select the label. Deterministic templates replace reasons that overstate support.
