# System Card Versus Model Card

The Step 23 service/view is the canonical interface over the Step 21 Project Model Card identity and data. Existing references remain compatible and new fields extend the record. A model is only one system component; rules, data, sources, interfaces, people, corrections, fallback, and retention also matter. Students maintain one card, not duplicates.
