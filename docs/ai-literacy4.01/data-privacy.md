# Data Privacy

Plans and project copies are private by default. Enforce least privilege, approved external processing, short retention, complete deletion, withdrawal, no model training by default, and content-free logs. Raw data are never uploaded merely to determine safety while the gate is unresolved.
