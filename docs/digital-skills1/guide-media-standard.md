# Guide Media Standard

Plain text is canonical. Each screenshot requires meaningful task-specific alt text, synthetic data and version review. Each video requires captions, matching transcript, visual-action descriptions, keyboard controls and no autoplay. Audio reuses Step 7, exactly matches visible text and never autoplays. Current screenshot/video pipelines are unavailable; complete text remains.
