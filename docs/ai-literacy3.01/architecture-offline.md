# Offline Architecture

Offline supports private drafts and low-fidelity planning only. It cannot call live services or pass an unverified gate. Reconnection validates scope, policy, permissions, versions, and ownership; conflicts preserve both histories and cross-account isolation.
