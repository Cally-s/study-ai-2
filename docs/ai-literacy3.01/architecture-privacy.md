# Architecture Privacy

Designs are private by default. Mark trust boundaries, block sensitive/unpermitted transfers, minimize logs, define retention/deletion, and prohibit model training by default. Do not store prompts, personal content, secrets, or stakeholder identities unnecessarily.
