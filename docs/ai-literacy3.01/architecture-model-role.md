# Model Role

Treat the model as one bounded component. Record actual metadata when known, allowed and prohibited tasks, tools, inputs/outputs, uncertainty, validation, human dependency, and fallback. Rule-based, searchable, human-led, and no-build designs require no model.
