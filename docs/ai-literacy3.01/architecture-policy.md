# Architecture Policy

Resolve assignment rules, provider-service permission, data permission, prototype restrictions, and external transfer before functional use. Unknown policy remains unknown and blocks affected paths. Architecture approval never authorizes deployment.
