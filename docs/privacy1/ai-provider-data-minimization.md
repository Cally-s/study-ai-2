# AI Provider Data Minimization

The Step 22 serializer removes diagnostic, disability, accommodation, medical, guardian, private-note, and unrelated-participant fields before provider use. Production must use an approved processor registry, minimal purpose-scoped inputs, verified retention/deletion configuration, contract and security/privacy review, and training disabled by default. No training opt-in is implemented. This prototype has no external AI or speech provider configuration to verify.
