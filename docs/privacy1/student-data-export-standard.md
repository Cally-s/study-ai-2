# Student Data Export Standard

Exports require category selection, authentication and step-up verification, subject/scope authorization, other-person filtering, private storage, rate limiting, an expiring link, SHA-256 hashes, and a manifest plus human-readable index. JSON must not be the only format. Highly restricted categories require explicit selection. No ordinary email attachment or internal storage path is allowed. No production export service exists here.
