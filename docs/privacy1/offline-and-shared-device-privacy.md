# Offline and Shared-Device Privacy

Step 27 device preferences require user/device namespace keys, validated schema migration and cleanup on account switch without removing educational drafts.

App-managed transcript, audio, practice, and offline-package caches may be cleared. Unsynced drafts block broad cleanup until exported or explicitly confirmed. Cleanup does not claim to remove Downloads-folder files, printouts, email copies, or other applications. Production must inventory IndexedDB, Cache Storage, service workers, tokens, outbox items, account namespaces, and sign-out cleanup.
## Recovery capacity drafts

Offline daily totals, windows, generic commitments and preferences remain user/session scoped. Calendar refresh is never fabricated or started automatically. Production must encrypt drafts, clear them at account switch/sign-out and reconcile versions without adding totals, duplicating commitments or exposing another account.
