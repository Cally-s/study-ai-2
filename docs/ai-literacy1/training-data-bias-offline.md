# Training Data and Bias Offline

The lesson and fixed synthetic outputs can be cached; owner-scoped audit drafts can be stored locally. Offline mode has no live generation, real-person ranking, external sharing, or competency decisions. Reconnection validates lesson/scenario versions, handles conflicts, prevents duplicates, and denies cross-account access.
