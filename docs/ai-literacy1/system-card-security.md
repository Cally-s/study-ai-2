# System Card Security

Use secret redaction, untrusted-content/prompt-injection boundaries, version and permission validation, a public-safe serializer, idempotency, tenant isolation, and privacy-safe content-free logs. Never expose keys, tokens, prompts, raw sensitive inputs, or exploit instructions.
