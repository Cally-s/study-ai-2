# Recovery Output Offline

A valid user-scoped cached output is labelled Offline with last-updated time. Student edits and partial progress persist; messages remain drafts, booking and activation are unavailable. Reconnection revalidates sources, detects conflicts, deduplicates snapshots and never communicates or activates automatically.
