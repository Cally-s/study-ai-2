# Printable Review Plans

Step 23 reuses the printable-plan generator. Batch work creates only version-linked printable drafts and never bypasses print preview or human approval.

Step 18 creates an immutable presentation snapshot of an existing StudySpark plan; it never creates or edits goals/tasks. Required title/course/goal/days/source version are preserved. Settings cover Letter/A4, Standard/Large Text/Extra Writing Space/Compact, language and writing lines. Browser printing requires explicit activation. Accessible PDF is unavailable and rejected. Exact ISO test dates/timezone are preserved; vague dates are rejected. Times are estimates. Names are omitted with a blank line by default. Browser/driver/AT tests remain pending.
Step 19 audio is independent of printable plan completion and cannot replace the visible/printable text source.
Step 20 printable guides reuse semantic text principles but do not create or modify any real study plan.
Step 21 Print Review Plan remains blocked for restricted/private content and otherwise requires a separate explicit Step 18 print workflow.
