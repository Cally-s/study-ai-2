# Printable Plan Versioning

Artifacts retain source plan ID/version, artifact ID/status, settings, creation/update dates and privacy-safe filename. The client snapshot is immutable. Comparing with a changed source marks the artifact Outdated, preserves the historical copy and forbids silent replacement. Durable artifact/template versions, settings hashes and storage require a production service.
