# Recovery Action Dependencies

Dependencies may be sequential, prerequisite, material, instruction, teacher-response, shared or explicitly parallel. A dependent action becomes ready only when every required predecessor is complete. The graph validator detects cycles before acceptance and structural changes.

Keyboard move controls preserve dependency checks. Shared actions are completed once. Step 12 remains the prerequisite source, and teacher replies are never assumed.
