# Missed Week Accessibility

The source contract uses semantic headings, ordered lesson/plan lists, native controls, visible focus, text status plus colour, reflow, forced-colour, reduced-motion, RTL, simplified/dyslexia-friendly/reading-ruler modes, low-bandwidth text and print rules. Dates include unambiguous text and lesson decisions expose reasons without hover. Browser, screen-reader, keyboard, 200% zoom/320px, language/RTL and representative-user execution remain `NOT_RUN`.
