# Accessibility Check Rule Registry

The registry derives one versioned definition for every Step 23 issue code. Each definition includes category, method, default severity, deterministic/heuristic status, draft support, human-review support, publication policy, evidence schema, remediation guidance and active version dates. Rule descriptions are canonical rather than copied into components. Organization policy—not a browser—decides which confirmed Critical/High issues block publication.
