# Academic Vocabulary Cards

Step 23 reuses the vocabulary registry for draft term identification and definitions. Suggested terms remain source-versioned drafts until academic review.

Status: APPROVED_FIXTURES_ONLY. Manual verification: PENDING.

Students explicitly open **Open academic vocabulary lookup**, then type/paste a term, use a section term list, or prefill from a native pointer selection. Selection alone never opens a card. Source wording/DOM remains visible, selectable, searchable, copyable and screen-reader accessible; words are not individually wrapped or made Tab stops.

Normalization uses Unicode NFKC, trims surrounding punctuation, preserves original form, meaningful hyphens/apostrophes and phrases, and classifies formulas, code identifiers, URLs, course codes and overlong fragments as ineligible. Resolution prefers exact term/phrase, alias/morphology and subject context. Ambiguous Function presents Mathematics and Computer Science choices instead of guessing.

Validated cards show English term, approved translation or honest missing status, subject, plain and academic definitions, part of speech, pronunciation metadata, example, related terms, paired misunderstanding/correction, source status, optional save, Listen and report. English remains primary. The reviewed fixture is Hypothesis—假设 with the required Science definitions and example.

The nonmodal labelled side panel supports Escape, focus return, scrolling, wrapped actions, narrow reflow, forced colours and 44px controls. TTS is explicit and never autoplays. Active assessments and hidden answers fail closed. No AI/dictionary/pronunciation provider exists; unknown data is never invented.
# Step 14 keyword control

Explain the English keywords extracts required contract terms and opens only approved Step 13 cards; it reports unresolved terms and never saves automatically. Definitions never use hidden assessment content.
# Step 15 vocabulary behavior

Text cards load before optional TTS and no pronunciation audio preloads. Session glossary is not durable offline and activities generate only on request. English remains immediately visible; low-bandwidth use is not a financial/ability signal.
Step 17 vocabulary components reuse reviewed term/sense versions and preserve English academic terms. Private glossary notes are excluded by default.
Step 18 keeps each vocabulary term with its definition and English academic-term metadata; private glossary notes are not printed by default.
Step 19 vocabulary narration derives from reviewed term/definition records and preserves the visible English academic term.
Step 21 Define Key Words selects the existing vocabulary-card presentation contract rather than inventing a separate glossary system.
