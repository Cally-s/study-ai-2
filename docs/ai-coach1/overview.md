# AI Coach Overview

Step 29 adds a persistent, server-enforced learning mode beside the composer. The selected mode governs future messages, remains visible, and appears on each response without changing earlier message history.
