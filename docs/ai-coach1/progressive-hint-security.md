# Progressive Hint Security

Trusted server mode snapshots, policy maximums, context hashes, row versions, idempotency, ownership/tenant checks, direct level schemas, tool filtering, cumulative disclosure, and Step 30 output validation protect transitions. Prompts, uploads, retrieval, tool output, client level values, double clicks, and offline replay cannot skip levels or reveal answers. Blocked candidates and private problem text are excluded from events.
