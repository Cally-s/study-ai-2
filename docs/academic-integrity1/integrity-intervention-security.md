# Integrity Intervention Security

Server checks actor, tenant, conversation, assignment, context, policy/version, category/reason, ladder action, mode, tool, preservation, concurrency, and idempotency. Student prompts, uploads, retrieved text, source pages, code, translations, and tool output cannot declare permission, enable a button, alter policy, generate hidden completion, run solvers, create alerts, or cross accounts.
