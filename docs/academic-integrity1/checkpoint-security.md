# Checkpoint Security

Trusted services must validate provenance, completion, policy version, source decisions, teacher review, tool permissions, assignment and conversation access, and tenant ownership. Prompt injection and retrieved content cannot complete checkpoints or alter provenance. Immutable versions, optimistic concurrency, idempotency, redaction, and deterministic safe fallbacks protect integrity.
