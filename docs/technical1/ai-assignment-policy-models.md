# AI Assignment Policy Models

`AIAssignmentPolicy` is the stable course-and-assignment identity. Immutable policy versions contain the effective window, disclosure/source requirements, final-answer rule, receipt requirement, and review requirement. Allowed modes and restricted actions are normalized child records. Only authorized teachers or administrators publish. `POLICY_UNKNOWN` always withholds final answers. Coach sessions retain the exact published policy-version ID used at start.
