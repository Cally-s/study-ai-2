# Lesson Offline Behaviour

Versioned student-safe lessons can be cached. Owner-bound response drafts are stored locally without evidence generation or sharing. Reconnection validates owner, tenant, lesson and phase versions, prevents duplicate responses/completion, preserves conflicts, and requires student review. This is a contract, not a deployed service worker.
