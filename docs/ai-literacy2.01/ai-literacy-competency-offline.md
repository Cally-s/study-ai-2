# Competency Offline Behaviour

The offline contract caches an owner/tenant/version-bound map and stores private evidence drafts. Drafts are never accepted and never change status offline. Reconnection validates owner, policy, competency, and source versions, prevents duplicates, preserves conflicts, and requires student review.
