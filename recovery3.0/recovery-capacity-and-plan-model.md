# Recovery Capacity and Plan Model

Capacity entries store local date, IANA timezone, exact windows/minutes, protected source text, fact status, confirmation and version. Overlap/duplicate/date normalization is validated. Immutable Plan Versions own Plan Days and action allocations. Minimum and Stretch tiers are distinct; scheduled minutes cannot consume nonnegative buffer. One active plan exists per case.
