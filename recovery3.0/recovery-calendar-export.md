# Recovery Calendar Export

Export requires plan review, calendar selection, exact event preview, title privacy, reminders, explicit confirmation and idempotent creation. Only selected focus blocks are eligible; protected buffer, commitments and private notes are not events.

This prototype offers a generic-title preview only. No approved connection exists, so writes fail safely and create no events. Calendar failure cannot change the plan or official school records.
