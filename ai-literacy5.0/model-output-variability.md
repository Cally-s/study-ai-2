# Model Output Variability

The same prompt can produce different results because of random generation, provider/model version, system configuration, tools, source packets, conversation context, language mode, or time. Regeneration is not prompt revision.

Where possible, comparisons record these variables. A single uncontrolled output difference cannot prove that a prompt change caused improvement.
