# Fairness Security

Treat test inputs/outputs and retrieved content as untrusted; protect prompt-injection paths, test-result integrity, sensitive-trait safeguards, versions, gate enforcement, idempotency, tenant ownership, and privacy-safe logs. Never allow fairness records to change production systems automatically.
