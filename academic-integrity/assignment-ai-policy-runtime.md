# Assignment AI Policy Runtime

Trusted server resolution loads the exact published assignment version, resolves inherited state and conditions, verifies attempt/source/review/final-answer gates, intersects tools and modes, builds model instructions and schemas, validates output, blocks non-compliant bodies, preserves drafts, and attaches an immutable snapshot to every governed message.
