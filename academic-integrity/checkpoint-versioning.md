# Checkpoint Versioning

Autosave preserves recovery state, while an intentional save or submission creates a formal immutable version. Reopening creates a new state. Material upstream changes mark downstream checkpoints for review without deleting artifacts. Historical provenance and decisions are never silently rewritten.
